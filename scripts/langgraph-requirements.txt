# Dépendances NeoKube — neokube/penpot-sidecar:v2.0
# Installées dans scripts/Dockerfile.penpot-sidecar

# ── LangGraph Embedded (graphe designer → seo_check) ─────────────────────────
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0  # SqliteSaver — persistance locale gratuite
langchain-core>=0.2.0               # Base partagée LangChain/LangGraph
langchain>=0.2.0                    # Utilitaires chaînes, messages, prompts

# ── LLM ───────────────────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── API / Web ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
requests>=2.31.0
pydantic>=2.0.0

# ── SRE Executor — Codestral (Mistral) + HuggingFace + K8s SDK ───────────────
mistralai>=1.0.0           # Codestral provider (API Mistral directe)
huggingface-hub>=0.22.0    # Codestral-22B-v0.1 via HuggingFace InferenceClient
kubernetes>=28.0.0         # SDK in-cluster pour K8sExecutor (optionnel, kubectl fallback)
psycopg2-binary>=2.9.0    # Auto-refresh token Penpot via PostgreSQL direct
