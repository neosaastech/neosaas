# Historique des actions Claude — neokube-beta

> Archivé depuis CLAUDE.md — 125 entrées, 2026-03-15 → 2026-05-13.
> Toutes les phases de sécurité (0–3) et capacités (4a–10d) sont ✅ terminées.

| Date | Action |
|---|---|
| 2026-05-13 | **feat(charlotte): streaming réponse finale + heartbeat toujours visible** — ReAct natural-finish path émettait `done` avec le texte complet sans `token` events → tout apparaissait d'un coup. Fix : `_final_streamed` flag ; pseudo-streaming mot par mot avant le `done` event (sauf synthesis path qui a déjà `_llm_call_stream`). Pipe heartbeat : `if _thinking_open: yield "·"` → `yield "·"` sans condition → les points `·` s'affichent même entre les step events (période de silence RAG ~9s). Antipattern #36 ajouté (regex builder ConfigMap sur clé data). Commits `39301db`, `6453b31`. |
| 2026-05-13 | **fix(charlotte): guard _is_charlotte_file + dédup kubectl -o (antipatterns #34+#35)** — Fix A : `_CHARLOTTE_OWN_FILES` frozenset réduit à 2 entrées (`serviceaccount-sre.yaml`, `sre_agent.py`) + helper `_is_charlotte_file(path)` basé sur le chemin complet (`"charlotte" in path` \| `"sre-script" in path` \| `basename in frozenset`). `configmap-neo-script.yaml` et tous les fichiers des autres agents ne sont plus bloqués. Fix B : `_kubectl_fingerprint(args)` normalise en ignorant `-o`/`--output` ; `_kubectl_seen` dict par session ReAct → `run_kubectl get pod x -o yaml` et `-o json` = même fingerprint = cache. RÈGLE ANTI-BOUCLE system prompt mise à jour. Antipatterns #33+#34+#35 + CLAUDE-agents.md règle #6 mis à jour. PR `fix/charlotte-guard-frozenset-kubectl-dedup`. Neo IMAP : PR `feat/neo-imap-resilience` (ImapClient + backoff 1s/3s/9s). |
| 2026-05-13 | **feat(charlotte): v3.15 — self-mod guard + anti-boucle + RBAC pods/exec** — Charlotte bloquée en boucle en tentant de s'améliorer elle-même (session Neo v1.1) ; 3 fixes : (1) RBAC `pods/exec create` ajouté au ClusterRole `agent-sre-role` ; (2) `_CHARLOTTE_OWN_FILES` frozenset + guard runtime dans `write_file` + `apply_gitops_fix` → `❌ AUTO-MODIFICATION BLOQUÉE` + ntfy `"🚫 tentative auto-modification"` (priority high) ; (3) prompt `RÈGLE AUTO-MODIFICATION — ABSOLUE` + `RÈGLE ANTI-BOUCLE` (stop tour 4/8 sans write + `ask_clarification` structuré + ntfy) ; `_write_done` flag dans ReAct loop ; CLAUDE-agents.md règle #6+#7 + antipattern #33 CLAUDE.md ; commit `322ab26` |
| 2026-05-13 | **feat(charlotte): quota fallback + LLM_CONV_MODEL + ntfy propre (antipattern #32)** — Quota Anthropic épuisé → session perdue silencieusement (session `ow-ef08d9726aa1`) ; 3 fixes : (1) `_llm_call` + `_llm_call_stream` détectent HTTP 402 / `"credit"` / `"insufficient"` → retry automatique avec `LLM_FALLBACK=mistral` + ntfy `"⚠️ Anthropic — crédits épuisés"` (rate-limitée 1/h, lien billing) ; (2) `LLM_CONV_MODEL=mistral-large-2407` pour classify + fast-path conversationnel (10× moins cher que claude-sonnet) ; (3) ntfy mission-end filtre `final.startswith("{")` (JSON brut) ; message erreur générique amélioré avec lien billing ; deployment-charlotte.yaml + commit `3bee404` ; R9.8+R9.9 + antipattern #32 documentés |
| 2026-05-13 | **feat(rag): neokube-architecture collection (251 chunks CLAUDE-*.md) + Charlotte wiring** — 8 fichiers CLAUDE-*.md indexés dans Qdrant `neokube-architecture` via `scripts/index-architecture-docs.py` (251/252 chunks) ; Charlotte /mission RAG block étendu : 3 collections maintenant interrogées par mission (sre-charlotte-incidents + neokube-process-docs + neokube-architecture, top_k=4, score=0.50) ; Temporal scan aussi augmenté ; log INFO `RAG architecture: N hits depuis neokube-architecture` ; ARCH_DOCS_COLL constant ; requirements.txt restauré dans sre-script (était perdu lors d'un rebuild yaml.dump) ; commit `2f36ed5` ; validé : Charlotte répond correctement `sre-script` à "quel est le nom exact du configmap ?" sans deviner |
| 2026-05-13 | **feat(leon): streaming réel SSE (antipattern #28 fix)** — `_build_sse(full_reply)` émettait 2 blocs statiques ; fast-path : `_conv_llm_stream(messages)` async generator, LiteLLM `stream=True`, token-par-token ; agent-path : `_stream_content(content)` mot-par-mot 8ms/token ; `oai_chat` → async def ; test validé 6+ chunks progressifs ; antipattern #28 Pattern B documenté ; commit GitOps |
| 2026-05-13 | **feat(charlotte): v3.14+ — trigger_dispatcher_workflow + signal_workflow** — Charlotte peut déléguer le pipeline métier au Dispatcher : `trigger_dispatcher_workflow(dev_project, spec)` lance `DevProjectWorkflow` (Aria+Nox+Vera+deploy), `trigger_dispatcher_workflow(check_status)` liste les workflows actifs, `signal_workflow(workflow_id, approve\|reject)` envoie `human_approved`/`human_rejected` ; Charlotte reste SRE pure — elle délègue le build/deploy sans l'absorber ; implémentations appelant `POST /trigger`, `GET /workflows`, `POST /approve/{id}`, `POST /reject/{id}` sur Dispatcher :8484 ; `kubectl replace sre-script` + rollout restart ; commit `c1f3b55` GitOps |
| 2026-05-13 | **feat(charlotte): v3.14 — LLM_MODEL=claude-sonnet (sessions) / LLM_SCAN_MODEL=mistral (scans)** — Coût 25€/48h non viable avec claude-sonnet pour tous les appels ; `_llm_call(model=)` paramètre ajouté ; `sre_analyze_with_llm` passe `model=LLM_SCAN_MODEL` (mistral, automatique) ; `/mission` endpoint garde `LLM_MODEL=claude-sonnet` (interactif) ; déploiement : `LLM_SCAN_MODEL=mistral` dans deployment-charlotte.yaml ; économie ~23$/jour estimée ; commit `a05f31e` GitOps ; validé startup `charlotte v3.14 démarrage — LLM_MODEL=claude-sonnet (sessions) LLM_SCAN_MODEL=mistral (scans Temporal)` |
| 2026-05-13 | **fix(charlotte): prompt v8 — anti-hallucination action + périmètre renforcé + pod stable** — Charlotte hallusinait "Modification appliquée" sur surfsense (hors périmètre, pod Running/0 restarts, format markdown) ; deux couches de fix : (1) `_NO_ACTION_NS` guard dans `apply_gitops_fix` (hard block retourne erreur) pour `{kube-system,security,monitoring,stalwart,penpot,dify,surfsense}` ; (2) prompt v8 : `confirmation_required=true OBLIGATOIRE même si l'humain demande "corrige directement"`, section `RÈGLE ANTI-HALLUCINATION D'ACTION` (interdiction d'écrire "appliqué" sans outil appelé, pod Running+0 restarts=aucun incident) ; push Langfuse v8 via `push-charlotte-prompt-v8.py` ; commit GitOps `sre-script` + rollout restart Charlotte ; antipattern #31 documenté |
| 2026-05-13 | **feat(dispatcher): Zoho → GitHub Issues sync (dispatcher_sync_zoho_issues)** — Audit agent révèle 4 écarts : 1 vrai (Charlotte sans règle Leon), 2 faux (Leon a scoring 8.2/10, zoho-observer P1+P2 déjà résolus), 1 vrai/nouveau (Zoho↔GitHub sync). Fix : Charlotte v7 avec règle "brief/spec/ProjectSpec → Leon" pushé Langfuse. Dispatcher : `_mcp_github()` helper, `MCP_GITHUB_URL`/`MCP_GITHUB_TOKEN` env, activity `dispatcher_sync_zoho_issues` — post-build non-bloquant — récupère tâches Zoho → crée GitHub Issues via `issue_write` MCP (label `zoho:{task_id}`, rétrolien commentaire Zoho) ; commits `2e98fbc`, `87bdbbf`, `ddff28a` |
| 2026-05-13 | **feat(charlotte): prompt v7 — règle périmètre Leon (brief/spec/ProjectSpec → Leon)** — Charlotte inventait des ProjectSpec et des briefs si on lui demandait. Fix : bloc "Gestion de projet, brief client, spec technique" ajouté dans section hors-périmètre → redirection Leon obligatoire ; +387 chars ; push v7 Langfuse ; `pull-charlotte-prompt.sh --apply` (GitOps auto-pusher) ; sync RAG |
| 2026-05-13 | **feat(penpot): connector v2.0 + comptes agents + identités visibles** — Création comptes `aria@neokube.fr` + `penpot@neokube.fr` dans DB Penpot (argon2id custom format) ; membres Neomnia Studio team (`82052e4a-*`) avec `can_edit=true` ; tokens JWE générés, stockés dans Vault v5 (`PENPOT_ACCESS_TOKEN`/`PENPOT_ARIA_TOKEN`/`PENPOT_AGENT_TOKEN`) ; penpot-connector v2.0 : auth prioritaire `PENPOT_ACCESS_TOKEN` → `as_agent` tokens → email/password fallback, champ `as_agent` pour switcher l'identité ; Aria `_penpot()` passe `as_agent="aria"`, agent Penpot passe `as_agent="penpot"` → agents visibles dans l'UI Penpot ; validé : `get-profile` retourne aria/penpot selon `as_agent` ; commit `2e98fbc` |
| 2026-05-13 | **fix(nox/aria/charlotte): ExceptionGroup MCP + SyntaxError f-string** — Bug critique : `raise RuntimeError()` à l'intérieur d'un `async with ClientSession()` + anyio TaskGroup → wrappé en ExceptionGroup → `except RuntimeError` ne catchait pas → activité Temporal plantait. Fix : stocker `_err/_text` dans les ctxmgr, raise/return après sortie — appliqué à `_mcp_github` (Aria + Nox) et `_mcp_neon` (Nox). Charlotte : `k8s_events_list({})` → `k8s_events_list({{}})` dans f-string system prompt → SyntaxError ligne 5878 → CrashLoopBackOff. E2E validé : Aria+Nox create_branch MCP ✅, Nox branche Neon + host ✅. commit `1cd9bdd` |
| 2026-05-13 | **fix(nox): openapi.yaml idempotent — SHA fetch avant create_or_update_file** — 409 "reference already exists" silencieusement ignoré si template-fastapi a déjà openapi.yaml sur la branche. Fix : `get_file_contents` pour récupérer SHA si le fichier existe → `create_or_update_file` avec SHA (update) ou sans SHA (create). Issues ouvertes revues : #1 résolu ici, #2 (Charlotte unreachable → ntfy fallback) déjà résolu, #3 (PENPOT_TEMPLATE_FILE_ID) déjà renseigné. commit `dda34fe` |
| 2026-05-13 | **feat(charlotte): prompt Langfuse v6 — MCP migration** — `connector-system` : ajout `github-mcp (port 8080, MCP streamable-http)` ; `agent-system` : ajout `k8s-mcp (port 8080, MCP streamable-http)` ; nouveau bloc `OUTILS K8S MCP` (k8s_* préfixés) listant 12 outils + règles lecture vs mutation ; `push-charlotte-prompt-v6.py` créé + exécuté ; `CLAUDE-charlotte-prompt.md` re-vectorisé via `pull-charlotte-prompt.sh --apply` ; commit GitOps `081683b` ; sync bidirectionnel Direction 1 + Direction 2 opérationnels |
| 2026-05-13 | **feat(mcp): Nox v3.0 — Neon MCP remote (Phase 1b complète)** — `_mcp_neon()` helper SSE (`mcp.neon.tech/sse`) avec `Authorization: Bearer NEON_API_KEY` ; `create_branch(projectId, branchName)` + regex `Branch ID: br-xxx` ; `get_connection_string(projectId, branchId)` + regex `@<host>/` pour extraire l'host ; `NEON_API_KEY` depuis `leon-neon-secrets` ; `NEON_CONNECTOR_URL` supprimé de Nox ; test E2E live sur `young-fog-76038471` confirmé (branch créée + host extrait + nettoyage) ; Nox est maintenant 100% MCP (GitHub MCP + Neon MCP) ; commit `fdcc477` |
| 2026-05-13 | **feat(mcp): Phase 2 — K8s MCP server + Charlotte v3.13** — Déploiement `kubernetes-mcp-server` (Red Hat/CNCF, `ghcr.io/containers/kubernetes-mcp-server:latest`) dans `agent-system` : 20 outils K8s (`pods_list`, `pods_log`, `events_list`, `resources_create_or_update`, `resources_scale`, etc.) ; Charlotte v3.13 : `mcp>=1.0.0` ajouté requirements.txt, imports `ClientSession + streamablehttp_client`, `MCP_K8S_URL` env var, `_init_k8s_mcp_tools()` (découverte dynamique au démarrage, non-bloquant), `_mcp_k8s_call()` (routage outils `k8s_*`), `_K8S_MCP_TOOLS` mergé dans `_MISSION_TOOLS + _K8S_MCP_TOOLS` ; commit `e352349` ; `kubectl replace sre-script` + rollout restart |
| 2026-05-13 | **feat(mcp): Phase 1 — GitHub MCP + Aria v3.0 + Nox v2.0** — Déploiement `github-mcp-server` (officiel GitHub v1.0.4) dans `connector-system` ; Aria v3.0 : template repo reste via github-connector, branch → `create_branch` MCP, push multi-fichiers → `push_files` MCP (atomique) ; Nox v2.0 : branch → `create_branch` MCP, OpenAPI stub → `create_or_update_file` MCP ; `github-mcp-token` Secret créé dans `connector-system` + `agent-system` ; commit `ccad832` |
| 2026-05-13 | **fix(charlotte): antipattern #29 récidive Rule 13 — {penpot.name}/{penpot.project_id}/{id[:8]} dans f-string** — System prompt Rule 13 avait 4 placeholders textuels non-échappés dans la f-string (`{penpot.name}`, `{penpot.project_id}` ×2, `{id[:8]}`) → `NameError: name 'penpot' is not defined` dès le premier appel à `dispatch_design_deploy` ; fix : `{{...}}` pour les 4 occurrences ; `kubectl replace` + `rollout restart agent-charlotte` ; commit GitOps `484fd92` ; CLAUDE-antipatterns.md #29 mis à jour |
| 2026-05-12 | **feat(agent-system): Gaps P1+P2+P3 résolus + comptes mail aria/nox + GitOps déployé** — zoho-observer v3.0 déployé : boucle C `_project_scan_loop` (300s) détecte statut "Prêt pour production", `zoho_to_project_spec()` mappe 13 champs depuis description structurée, idempotence via `_has_bot_comment()` ; Dispatcher v2.0 : email HTML 7 étapes `dispatcher_send_client_mail(spec, report)`, `percent_complete=100` ferme tâche Zoho en fin de workflow ; Stalwart : comptes `aria@neokube.fr` + `nox@neokube.fr` créés (`roles: ["user"]`), secret K8s `agent-mail-secrets` créé ; deployment-aria/nox : `MAIL_FROM` + `MAIL_PASSWORD` ajoutés ; commit GitOps `b2da695` ; Gaps P1/P2/P3 CLAUDE-pipeline.md mis à jour ✅ |
| 2026-05-12 | **feat(zoho-observer): signalisation complète Open→Closed + idempotence cross-restart** — zoho-observer : `_has_bot_comment()` vérifie commentaire 🤖 avant dispatch (idempotence cross-redémarrage) + commentaire enrichi (agent, workflow_id, timestamp, lien Temporal) ; Dispatcher : `dispatcher_zoho_callback()` ferme la tâche après commentaire (`percent_complete=100` → Closed) ; `PenpotToVercelWorkflow` appelle callback en fin si `zoho_task_id` présent ; `/trigger-penpot` accepte `zoho_task_id`/`zoho_project_id` ; statuts Zoho portail confirmés : Open (…068) + Closed (…071, via pct=100), pas de In Progress ; commit `7f5e3e1` |
| 2026-05-12 | **fix(charlotte): penpot.project_id absent de project_health_check + Rule 13 séquence 3 étapes** — `_check_penpot()` calculait `pid` mais ne l'incluait pas dans le dict retourné → Charlotte ne pouvait jamais récupérer l'UUID du projet Penpot par nom ; fix : `"project_id": pid` ajouté au résultat ; Rule 13 renforcée : a) `project_health_check` cherche le projet par nom, b) `ask_clarification` confirme avec l'utilisateur avant de déclencher, c) `dispatch_design_deploy` seulement après confirmation ; antipattern #30 documenté ; commit `cc35d37` |
| 2026-05-12 | **feat(pipeline): Penpot → GitHub → Vercel — Phase 3b complète** — `PenpotToVercelWorkflow` ajouté au Dispatcher (4 étapes : aria_export_penpot → aria_generate_nextjs → aria_push_to_github → redeploy Vercel) ; endpoint `POST /trigger-penpot` ; Aria v2.0 : 3 nouvelles activités Temporal (`aria_export_penpot`, `aria_generate_nextjs`, `aria_push_to_github`) + helpers `_penpot()`, `_llm()`, `_extract_design_brief()` ; Charlotte : outil `dispatch_design_deploy` + Rule 13 système prompt ; 3 ConfigMaps appliqués (kubectl apply aria/dispatcher, kubectl replace charlotte >262KB) + rollout restarts ; CLAUDE-pipeline.md Phase 3b + CLAUDE-agents.md flux Design→Code mis à jour |
| 2026-05-12 | **feat(charlotte): outil trigger_vercel_deploy + Rule 12** — Charlotte écrivait des tutoriels manuels Vercel au lieu d'agir ; outil `trigger_vercel_deploy` ajouté : récupère le dernier déploiement via vercel-connector, déclenche `/v12/deployments/{uid}/redeploy` ; Rule 12 dans system prompt : INTERDIT de décrire les étapes manuelles, déclencher DIRECTEMENT ; commit `6379fa6` GitOps |
| 2026-05-12 | **fix(charlotte): NameError `name 'agent' is not defined` — f-string system prompt** — `{agent}` au lieu de `{{agent}}` dans 4 occurrences du protocole 6 étapes (system f-string) ; Python évaluait `agent` comme variable → NameError à chaque appel `/mission` ; fix : `{agent}` → `{{agent}}` ; `kubectl replace -f` (ConfigMap 291KB) + rollout restart Charlotte ; commit `bb9c154` GitOps |
| 2026-05-12 | **security: incident Scaleway — accès frauduleux 2 856€** — 10 serveurs Elastic Metal (EM-I620E-NVME/EM-B520E-NVME) provisionnés sans autorisation par `repugnant93@gmail.com` (5-12 mai) depuis IPs Shadow SAS 185.161.169.x ; root cause : clé API SCWK4R47320WGS3R5GWA leakée avant migration Vault ; actions : 10 serveurs supprimés, clé attaquant SCW6BJE65EXH4ZDQGCWD supprimée, IAM policy durcie (2 règles : project-scope 11 perms + org-scope BillingReadOnly), cluster Kapsule inutile `neokube-rag` supprimé (~30€/mois économisés), CronJob `scaleway-billing-alert` créé (alerte ntfy si >200€, critique >350€, 8h Paris) ; dossier contestation + rapport incident créés (`dossier-contestation-scaleway.md`, `scaleway-incident-report-2026-05-12.md`) ; ticket Scaleway à ouvrir manuellement sur console.scaleway.com/support |
| 2026-05-12 | **refactor(charlotte): philosophie primitives génériques — kubectl_apply remplace maintenance_pod** — maintenance_pod (outil ad-hoc PVC) supprimé ; kubectl_apply (applique tout manifest YAML via admin-sys /apply) ajouté ; delete débloqué dans run_kubectl (admin-sys est la frontière de sécurité réelle) ; antipatterns #23/24/25 documentés dans CLAUDE-antipatterns.md ; CLAUDE-agents.md mis à jour (table outils, philosophie primitives, admin-sys v5.0) ; commit `cd0bc54` |
| 2026-05-12 | **feat(charlotte): outil maintenance_pod + réponse garantie v3.12** — Charlotte n'avait aucun outil pour supprimer des fichiers sur un PVC (nécessaire pour corriger zero-cache) → ajout `maintenance_pod` (crée pod busybox éphémère avec PVC, exécute commande, retourne logs, supprime) ; contexte outil réduit 8000→2500 chars pour éviter timeout Mistral sur sorties volumineuses ; règle 5 mise à jour : Charlotte DOIT toujours donner un diagnostic même si elle ne peut pas corriger ; pattern zero-cache documenté dans charlotte-reference.md ch.8 + system prompt (symptômes → fix autonome obligatoire) ; commit `6760bb6` |
| 2026-05-12 | **analyse(charlotte): pas de réponse sur zero-cache** — root cause : 5 outils tour 1 × 8000 chars = 40KB contexte → LLM Mistral timeout au tour 3 → synthesis fallback échoue aussi → emit final non reçu (SSE timeout OWU) ; Charlotte n'avait pas `maintenance_pod` donc ne pouvait pas corriger zero-cache même si elle diagnostiquait correctement ; règle 5 contradictoire ("ne donne jamais de commandes") empêchait même le fallback de diagnostic ; toutes ces causes corrigées dans commit `6760bb6` |
| 2026-05-12 | **fix(surfsense): zero-cache CrashLoopBackOff 21 restarts — réplication corrompue** — root cause : état de réplication SQLite corrompu (transaction `5sqn6bs` en boucle, WebSocket 1009 message trop grand) → mémoire native SQLite hors heap V8 croissait jusqu'à OOMKill à 5Gi malgré `--max-old-space-size=4096` ; fix : pod de maintenance `zero-cache-cleaner` supprime `zero.db` (124MB) + fichiers WAL sur PVC → rollout restart → snapshot propre depuis Postgres ; résultat : 470Mi stable (vs OOMKill à 5Gi avant) ; commit `c3a01ae` |
| 2026-05-12 | **fix(charlotte): guard kubectl logs pod périmé** — Charlotte utilisait un nom de pod issu des events ou du describe (ex: `surfsense-zero-cache-56c47fbb4-vzrj5` inexistant) au lieu du pod actuel (`5f65b49769-bcm8q`) ; guard runtime dans `run_kubectl` : si verb=logs et pod non trouvé → retourne erreur instructive "utilise kubectl get pods -l app=<name>" ; commit `c3a01ae` |
| 2026-05-12 | **fix(charlotte): guard placeholders kubectl v3.12 déployé** — detection runtime des args contenant `<[A-Z_]+>` dans `run_kubectl` : retourne `[ERREUR INTERNE]` instructive au lieu d'exécuter une commande cassée ; déclenché par cas où Charlotte avait le vrai nom de pod (`open-webui-695f895b48-pfvl4`) mais appelait kubectl avec `open-webui-695f895b48-<POD_NAME>` ; le guard force la substitution des vraies valeurs sans programmer chaque cas ; commit `9a334af` GitOps ; Charlotte v3.12 active |
| 2026-05-12 | **fix(litellm): tarification Mistral corrigée — Cloudflare AI Gateway cache 88%** — LiteLLM estimait Mistral ×8 la réalité (spend LiteLLM $31 vs €3.58 réel Mistral) ; root cause : Cloudflare AI Gateway cache ~88 % des appels SRE répétitifs, mais LiteLLM comptabilise le plein prix pour chaque appel entrant ; fix : `input_cost_per_token: 0.00000024` + `output_cost_per_token: 0.00000072` dans `configmap-litellm-config.yaml` (×0.12 du tarif officiel) ; budget virtual key Charlotte réinitialisé ($30→$50) ; budget déclenché = Charlotte bloquée ntfy spam toutes les 5min pour `anthropic=quota_exceeded` (OpenAI aussi gaspillé : Charlotte utilisait `gpt-4o` par défaut — `LLM_MODEL` corrigé en `"mistral"`) |
| 2026-05-12 | **feat(cockpit): rapport hebdo coûts LLM — llm-key-validation lundi 6h** — `get_openai_monthly_cost()` : coûts réels OpenAI via `GET /v1/organization/costs` (bucket daily) ; `get_litellm_spend()` : spend estimé par agent via `/key/list` + `/key/info` LiteLLM ; ntfy rapport lundi 6h : tableau par agent avec barre de progression budget + note "Mistral ×0.12" ; schedule `0 6 * * *` → `30 6 * * *` (shift pour éviter conflit llm-key-sync) |
| 2026-05-12 | **fix(charlotte): cooldown 2h sur sre_trigger_llm_key_sync** — sans cooldown, Charlotte re-déclenchait `llm-key-sync` toutes les 5min (SREScanWorkflow) pour `anthropic=quota_exceeded` → spam ntfy + appels cronjob inutiles ; fix : vérifie l'heure du dernier job `llm-key-sync-sre` avant de créer un nouveau → `SKIPPED_COOLDOWN` si < 2h |
| 2026-05-12 | **fix(surfsense): zero-cache mémoire 7.1 Gi → 1.5 Gi — heap V8 corrigé** — root cause : `--max-old-space-size=10240` désactivait le GC agressif de V8 → accumulation passive jusqu'à 7.1 Gi malgré une `zero.db` replica de 124 MB ; fix : `--max-old-space-size=4096` force le GC dès 4 Gi ; `limits.memory: 12Gi → 5Gi`, `requests.memory: 2Gi → 1Gi` (snapshot initial nécessite > 2 Gi — OOMKilled à 2 Gi) ; mémoire cluster : 76 % → 53 % ; antipattern §14 mis à jour |
| 2026-05-12 | **fix(surfsense): celery mémoire** — `limits.memory: 16Gi → 2Gi`, `requests.memory: 1Gi → 512Mi` (sans charge, ~350 MB steady-state) |
| 2026-05-12 | **fix(charlotte): hallucination réponse infra sans vérification** — symptôme : Charlotte inventait des valeurs (`--max-old-space-size=6144`, `CACHE_CHUNK_SIZE=1024`) pour répondre à "as-tu appliqué cette nouvelle version ?" sans appeler le moindre outil ; root cause 1 : "appliquer"/"version" absents de `_SRE_KW` → fast-path conv → Charlotte répond sans outils ; root cause 2 : `tool_choice: "auto"` au tour 0 du loop → LLM peut répondre en texte sans appeler d'outil ; fix 1 : `_SRE_KW` étendu (verbes d'action infra : appliqu/version/update/config/yaml/rollout/migrat…) ; fix 2 : `tool_choice: "required"` au tour 0 ; commits `06f641e` + `c6dbc0a` |
| 2026-05-12 | **feat(charlotte): routing sémantique LLM v3.12 — remplace _SRE_KW** — `_SRE_KW` (50+ mots-clés fragiles) remplacé par un appel LLM `temperature=0.0, max_tokens=5` qui classe "sre" ou "conv" ; résistant aux paraphrases, langues mixtes, formulations implicites ; bypass automatique si Charlotte a posé une question en attente (`"?"` dans le dernier message assistant → route `sre` directement) ; tracé dans Langfuse sous `charlotte-classify-{session_id}` ; commit `3398687` |
| 2026-05-12 | **feat(charlotte): outil ask_clarification — dialogue avant action ambiguë v3.12** — Charlotte peut suspendre le ReAct loop pour poser une question avant d'agir sur une ressource ambiguë ou irréversible (valeur non précisée, plusieurs cibles, action sans confirmation) ; implémenté comme outil dans `_MISSION_TOOLS` : quand appelé, la question devient la réponse finale du tour + loop break ; prochain message de l'utilisateur : classifieur détecte `"?"` → bypass → `sre` direct → Charlotte reprend avec le contexte complet ; règle 6b ajoutée au system prompt ; antipattern #22 ajouté |
| 2026-05-12 | **fix(charlotte): requirements.txt absent après kubectl replace** — bug de procédure : `kubectl replace -f cm.yaml` supprime toutes les clés non présentes dans le fichier fourni ; la première tentative n'incluait que `sre_agent.py` → `requirements.txt` supprimé → CrashLoopBackOff `ModuleNotFoundError: httpx` ; fix : toujours inclure `requirements.txt` + `sre_agent.py` dans le ConfigMap ; antipattern #22 documenté |
| 2026-05-07 | **fix(charlotte): chemin conversationnel OWU — detection + system prompt** — `_SRE_KW` : noms d'agents retirés ("charlotte", "leon", etc.) qui matchaient les salutations ; `_msg_lower` : première ligne seulement (`message.split('\n')[0][:200]`) pour ignorer le contexte Code Interpreter qu'OWU ajoute après le message réel ; chemin conversationnel : system prompt SRE remplacé par prompt léger amical (Mistral répondait "Je vérifie le cluster" même pour "bonjour") ; `kubectl replace` (ConfigMap >262KB) |
| 2026-05-07 | **fix(charlotte): LLM_MODEL mistral + fix JSON tool_calls vide + tool_choice auto** — Charlotte renvoyait 401 sur `mistral-large-2407` (virtual key LiteLLM autorise alias `mistral` uniquement) ; `LLM_MODEL: "mistral"` dans deployment-charlotte.yaml ; bug `tool_choice: "required"` → Mistral forcé d'appeler un outil même pour "bonjour" → corrigé en `"auto"` ; bug `tool_calls: []` → `final = '{"tool_calls": []}` visible utilisateur → fallback LLM sans outils ajouté |
| 2026-05-07 | **fix(neo): streaming réel OWU + TTFT 6.7s→0.56s** — Neo attendait la réponse complète puis envoyait en un bloc ; streaming réel : `httpx.AsyncClient.stream()` SSE pass-through ; détection `_TOOL_KW` pour chemin direct sans outils ; `_remember()` rendu non-bloquant avec `asyncio.create_task()` ; endpoint `/models` alias de `/v1/models` ajouté (OWU appelle les deux) ; `LLM_FALLBACK` corrigé dans deployment-neo.yaml (gemini-flash épuisé → mistral) |
| 2026-05-07 | **fix(surfsense): zero-cache OOM root cause + GitOps correct** — root cause V8 heap limit (2 sous-processus Node.js indépendants dans rocicorp/zero 0.26.x, change-streamer crashait à 6Gi) ; fix : `NODE_OPTIONS=--max-old-space-size=10240` + `limits.memory: 12Gi` + probes élargies ; commit `af3db45` GitOps — pod stable à ~6Gi RAM ; serveur Ubuntu crashé avant le git push de Charlotte, bootstrap avait reverté le patch live |
| 2026-05-07 | **feat(charlotte): outil atomique `apply_gitops_fix`** — outil 4 étapes atomique dans `configmap-sre-script.yaml` : `write_file` → `kubectl apply/replace` → `verify_pod_healthy` → `git_push` (3 retries + backoff exponentiel) ; rollback automatique sur échec apply/verify ; alerte ntfy ÉTAT DÉGRADÉ si tous les push échouent ; prompt mis à jour : `apply_gitops_fix` = méthode préférée ; `kubectl replace` pour sre-script ConfigMap (>262KB, `kubectl apply` refusé) ; documentation G1/G2 + antipatterns #15/#17/#18 CLAUDE.md |
| 2026-05-07 | **fix(neo): auto-enregistrement Open WebUI + R9 compliance** — Neo n'était jamais dans OWU (setup initial manquant, pas une régression) ; fix : endpoint `/v1/models` ajouté + `_owu_get_token()` + `_owu_self_register()` (pattern Charlotte) dans lifespan ; ConfigMap : `OPENWEBUI_URL`, `VAULT_OPENWEBUI`, `VAULT_ADDR` ajoutés ; `LLM_MODEL` : `gpt-4o`→`mistral-large-2407`, `LLM_FALLBACK` : `mistral-large-latest`→`gemini-flash` (R9) ; bug corrigé : `@asynccontextmanager` déplacé sur mauvaise fonction → CrashLoopBackOff ; commits `701ffff` + `1d5d6a4` |
| 2026-05-07 | **docs(agents): checklist OWU self-registration obligatoire** — étape `6b` ajoutée à la checklist intégration agent : ConfigMap entries (`OPENWEBUI_URL`, `VAULT_OPENWEBUI`), endpoint `/v1/models`, `_owu_get_token()` + `_owu_self_register()` (référence neo.py v1.2/Charlotte) ; template Deployment §3 mis à jour avec les 2 nouvelles env vars ; règle : enregistrement non bloquant (exception catchée) |
| 2026-05-06 | **fix(charlotte): namespaces corrects v2 + système prompt v3/v4** — system prompt corrigé dans les 3 emplacements du configmap : `agent-ops`→`agent-system`, `open-webui`→`interfaces`, `vault`→`security` ; v3 : périmètre strict ✅/🚫/⛔ (kube-system hors périmètre, chmod/rollout undo interdits) ; v4 : `confirmation_required: bool` + `questions: []` pour les décisions stratégiques ; Langfuse `charlotte-sre` v2/v3/v4 enregistrés ; score Charlotte : 8.2→9.17/10 |
| 2026-05-06 | **feat(charlotte): streaming /mission/stream SSE + Pipe générateur Open WebUI** — `_mission_queues` dict + `_emit()` helper ; 6 points d'émission dans `mission()` ; endpoint `/mission/stream` SSE (`StreamingResponse`, asyncio.Queue, timeout 300s) ; Pipe Open WebUI changé de `-> str` à générateur avec `yield` (events : step/tool/result/done/error) ; Pipe utilise `requests.post(stream=True)` + `iter_lines()` |
| 2026-05-06 | **fix(charlotte): 2 bugs post-streaming — NameError {text}/{preview} + EVAL_WATCH_EVERY_N manquant** — Bug 1 : f-strings `{text}` et `{preview}` non échappés dans `_gen_charlotte_pipe_code` → crash au démarrage (`NameError`) ; correction : `{{text}}` et `{{preview}}` ; Bug 2 : constante `EVAL_WATCH_EVERY_N` utilisée dans le Bloc E du SREScanWorkflow mais jamais définie → crash Temporal ; correction : `EVAL_WATCH_EVERY_N = int(os.getenv("EVAL_WATCH_EVERY_N", "6"))` ajouté au module level |
| 2026-05-06 | **docs(charlotte): charlotte-reference.md — référence opérationnelle 9 chapitres** — `docs/charlotte-reference.md` créé dans Kubinote-GitOps (monté `/gitops` dans le pod) ; 9 chapitres : Cluster & Namespaces, Agents & Rôles, Périmètre d'action, Services critiques, Gouvernance LLM R9, SREScanWorkflow Blocs A-E, Connectors & Outils, Procédures de remédiation, Anti-patterns SRE ; lisible via `read_file("/gitops/docs/charlotte-reference.md")` ; 10 chunks vectorisés dans `neokube-process-docs` (hook commit auto) |
| 2026-05-06 | **fix(langfuse): LANGFUSE_PUBLIC_KEY — root cause traces vides** — clé publique hardcodée incorrecte dans `configmap-litellm-config.yaml` (`pk-lf-neokube-65c7fe8ce2894d7e`) et en valeur statique dans tous les deployments agents ; corrigé : configmap utilise `os.environ/LANGFUSE_PUBLIC_KEY`, deployments utilisent `secretKeyRef: cluster-manager-secrets: LANGFUSE_PUBLIC_KEY` ; correcte clé `pk-lf-b1a84594-a9c9-453a-bdec-a511d12e060f` désormais injectée depuis Vault via cockpit-secrets |
| 2026-05-06 | **fix(agents): fallbacks LLM temporaires** — Charlotte + Penpot : `claude-sonnet` → `mistral` (compte Anthropic épuisé, clé sk-ant-api03-RNfT... révoquée après exposition) ; Dispatcher + Domi : `gemini-flash` → `mistral` (quota Gemini gratuit épuisé, reset quotidien) ; tous les agents opérationnels sur `mistral` en attendant rechargement des comptes ; R9 mis à jour dans CLAUDE.md avec modèles cibles et procédure de restauration |
| 2026-05-06 | **feat(eval): agent_eval.py v1.0** — script LLM-as-judge pour les 9 agents NeoKube (`~/scripts/agent_eval.py`) ; `AgentSpec` dataclass par agent (modèle, prompt Langfuse, fallback, 3 scénarios) ; juge `mistral` via LITELLM_MASTER_KEY (cockpit-secrets) ; scores Langfuse envoyés par trace ; résolution credentials 100% K8s (`_kubectl_secret()`, antipattern R13) ; scores baseline : Domi 9.7, Penpot 9.6, Vera 9.2, Aria 9.1, Charlotte 8.2, Leon 8.2, Nox 8.0, Dispatcher 7.6, Neo 7.7 |
| 2026-05-06 | **feat(eval): reindex_neo_knowledge.py** — ré-indexation `neokube-process-docs` Qdrant : 70 chunks depuis CLAUDE.md (H2/H3) remplacent 91 anciens vecteurs nomic-embed-text-v1 (scores ~0.04) ; modèle actif `paraphrase-multilingual-mpnet-base-v2` via alias LiteLLM `nomic-embed-text` ; scores post-fix ~0.59 ; Neo score RAG : 6.1 → 7.7/10 ; credentials via `_kubectl_secret(cockpit-secrets, cockpit, LITELLM_MASTER_KEY)` |
| 2026-05-06 | **feat(langfuse): prompts Dispatcher + Nox enregistrés** — `dispatcher-orchestrator` v1 : flux 9 étapes dans l'ordre + `return_exceptions=True` explicite + Penpot/Domi non-bloquants + Vera rejected = avertissement (pas arrêt) ; `nox-backend` v1 : contrainte NeoBridge (POST /projects bloqué, org managed by Vercel), pattern branche-par-projet sur `young-fog-76038471` |
| 2026-05-06 | **feat(neo): system prompt v2 — URLs NeoKube de référence** — section "INFRASTRUCTURE NEOKUBE — URLS DE REFERENCE" ajoutée : 11 URLs publiques neokube.fr + 4 URLs internes neokube.local + stack agents (ports) ; configmap K8s mis à jour + Langfuse `neo-assistant` v2 ; Neo rollout restart déclenché ; score URL webmail : 4.0 → 9.0/10 ; score global : 7.7 → 8.5/10 |
| 2026-05-06 | **fix(eval): prompts Langfuse Dispatcher + Nox** — `agent_eval.py` mis à jour : `langfuse_prompt_name` renseigné pour dispatcher (`dispatcher-orchestrator`) et nox (`nox-backend`) ; fallback prompts enrichis avec contraintes critiques ; scores post-fix : Dispatcher 7.6 → 10.0, Nox 8.0 → 8.9 |
| 2026-05-06 | **feat(eval): agent-eval-nightly CronJob + Charlotte sre_check_eval_scores** — CronJob `agent-eval-nightly` (agent-system, 2h Paris) : 3 scénarios × 9 agents, virtual key dédiée `eval-judge` (mistral, 10€/mois), score `eval_avg` → Langfuse, alerte ntfy si avg < 7.5 (EVAL_THRESHOLD) ; Charlotte Bloc E `sre_check_eval_scores` : poll rolling avg Langfuse (3 derniers runs), ntfy high si agent < 7.0, intervention auto llm-key-sync via admin-sys ; fix sre_ntfy_notify manquant dans liste activities Charlotte |

| Date | Action |
|---|---|
| 2026-03-15 | Reprise de la synchronisation `Production-clients` via `rclone bisync` |
| 2026-03-15 | Création de ce fichier `CLAUDE.md` |
| 2026-04-21 | Intégration RAGAS 0.4 + Langfuse — script `rag_eval.py` |
| 2026-04-25 | Fix post-restart : recréation namespaces Temporal `sre-charlotte` + `zoho-integration` ; fix `llm-key-sync` (`python3` → `jq`) persisté en GitOps |
| 2026-04-25 | Architecture persistence cluster : Temporal emptyDir → PVC `/projets/temporal` (5Gi) + namespaces dans flags `start-dev` ; namespace `management` formalisé dans GitOps ; CronJob `cluster-bootstrap` (*/5 min, idempotent) dans `apps/management/base/` ; règle P8 ajoutée au carnet de processus |
| 2026-04-25 | Migration Vault `vault` → namespace `security` (Helm + données Raft copiées) ; vault-unsealer mis à jour |
| 2026-04-25 | Ajout ttyd (terminal web) dans namespace `interfaces` — `tsl0922/ttyd:1.7.7`, ingress `ttyd.neokube.local` |
| 2026-04-25 | Migration embedding Ollama → HuggingFace : suppression Ollama (`-8Gi RAM requests`) ; LiteLLM `nomic-embed-text` → `paraphrase-multilingual-mpnet-base-v2` via HF router gratuit (768 dims, multilingue) ; `HUGGINGFACE_API_KEY` ajouté dans `cockpit-secrets` |
| 2026-04-25 | Déploiement Dify v1.13.3 dans namespace `dify` — 7 composants dont `dify-plugin-daemon:0.5.3-local` (requis Dify v1.x) ; fix permissions storage (initContainer chown UID 1001) ; DB `dify_plugin` créée ; ingress `dify.neokube.local` uniquement |
| 2026-04-26 | **Phase 0** : suppression `ClusterRoleBinding agent-sre-cluster-admin` ; `agent-sre-role` restreint ; AgentSpec charlotte v2.5 ; AgentSpec leon v2.0 (Chef de Projet, `forbidden_actions`, `output_schema` ProjectSpec) |
| 2026-04-26 | **Phase 2** : sidecars `tool-validator` (port 8090) + `output-guard` (port 8091) sur Charlotte et Leon ; `configmap-agent-policies` (allowlist 10 outils Leon, 26 forbidden) ; hooks dans `_execute_tool` + `run_agent` + `_mission_execute_tool` + `POST /mission` |
| 2026-04-26 | **Phase 1** : namespace `connector-system` ; `zoho-connector` complet (OAuth2 Vault + proxy `/proxy`) ; stubs `github-connector` + `vercel-connector` ; Charlotte migrée (`_zoho_api` → zoho-connector) |
| 2026-04-26 | **Phase 1b** : `github-connector` v1.0 (proxy GitHub REST API, token `GITHUB_TOKEN` depuis `github-connector-secrets`) ; `vercel-connector` v1.0 (proxy Vercel API, `VERCEL_TOKEN` + `VERCEL_TEAM_ID` depuis `vercel-connector-secrets`, teamId injecté auto) ; Leon — toutes les activités Zoho/GitHub/Vercel migrées vers leurs connecteurs respectifs ; `leon_zoho_refresh_token` → no-op ; deployment Leon nettoyé (secrets Zoho/GitHub/Vercel directs supprimés, URLs connectors en env) |
| 2026-04-26 | **Vault fix** : tous les connectors lisent depuis Vault (secret `vault-root-token` créé dans `connector-system`) ; `secret/neokube/infrastructure/{github,vercel,neon}` créés depuis les K8s secrets existants |
| 2026-04-26 | **Phase 3** : `neon-connector` v1.0 (port 8003) — proxy Neon Management API (`/proxy`) + exécution SQL via asyncpg (`/query`) ; Leon — 5 activités Neon migrées ; secrets `leon-neon-secrets` directs supprimés du deployment |
| 2026-04-26 | **Phase 4c** : Charlotte → zoho-connector câblé — `ZOHO_CONNECTOR_URL` dans deployment-charlotte.yaml + charlotte-config ; `known_tools` documenté dans agent-policies |
| 2026-04-26 | **Phase 4b** : `sre_agent_health_matrix` — snapshot pod/CPU(top)/RAM/probe HTTP par agent actif du registre ; fallback GitOps si K8s indisponible ; SREScanWorkflow étape 8 (`agent_matrix` dans report) ; endpoint `GET /agents/health` ajouté |
| 2026-04-26 | **Phase 4a — DT-005** : `sre_qdrant_check_prior_remediation` (scroll exact + sémantique sur `remediation_outcome`) gate le loop drifts dans `SREScanWorkflow` — action ESCALATE si `failed_before` ; `sre_vectorize_remediation_outcome` écrit chaque PATCH/PATCH_FAILED en retour dans Qdrant pour alimenter les cycles suivants |
| 2026-04-26 | **Audit roadmap** : vérification doublons — `sre_provision_agent` + `sre_decommission_agent` déjà implémentés (ProvisionAgentWorkflow/DecommissionAgentWorkflow) ; ProjectSpec déjà défini dans leon.yaml ; agent-registry v1.3 (charlotte 2.5, leon 2.0) ; 4 collections Qdrant Phase 5 ajoutées (statut `planned`) ; charlotte.yaml : shared_secrets Zoho supprimés → connector déclaré ; leon.yaml : optional_keys nettoyés (Phase 1b/3 terminées) |
| 2026-04-27 | **Phase 5** : Dispatcher v1.0 déployé — `configmap-dispatcher-script.yaml` (DevProjectWorkflow + 7 activités : validate/aria_build/nox_build/vera_review/notify_approval/deploy/write_pm_decisions) ; pod `dispatcher` port 8484, namespace Temporal `dispatcher` ; sidecars tool-validator + output-guard ; AgentSpecs Aria/Nox/Vera + dispatcher créées ; registre v1.4 ; 4 collections Qdrant `active` |
| 2026-04-27 | **Phase 6** : Aria/Nox/Vera découplés en pods dédiés — scripts `configmap-aria-script.yaml` / `configmap-nox-script.yaml` / `configmap-vera-script.yaml` ; deployments + services + serviceaccounts dédiés (ports 8485/8486/8487) ; task_queues séparées (`aria-queue`/`nox-queue`/`vera-queue`) dans DevProjectWorkflow ; dispatcher-script.yaml mis à jour (ARIA/NOX/VERA_QUEUE env vars + task_queue dans execute_activity) ; policies Aria/Nox/Vera ajoutées ; registre v1.5 (health_url + task_queue par agent) ; bug fix deployment `dispatcher_agent.py` → `dispatcher.py` |
| 2026-04-27 | **fix(leon)** : `dispatch_project` tool ajouté — Leon → `POST /trigger` Dispatcher avec ProjectSpec 11 champs ; `DISPATCHER_URL` dans leon-config + leon-script ; `new_project` marqué [LEGACY] ; pipeline Leon → Dispatcher désormais fonctionnel |
| 2026-04-27 | **fix(backup)** : `dump-mongodb` init container supprimé — MongoDB retiré du cluster, le `set -e` bloquait tout le job nightly ; TIMESTAMP déplacé dans le container upload ; backup vérifié OK (Penpot+OpenWebUI+Qdrant+GitOps) |
| 2026-04-27 | **fix(charlotte/git)** : `git_status` corrigé — fetch `origin/main` + `log origin/main..HEAD` pour exposer les commits non pushés (Charlotte disait "à jour" avec 23 commits locaux en attente) ; `_git_pull()` : `fetch --depth=20 + reset --hard` → `fetch + rebase` (préserve les commits locaux non pushés) ; 27 commits pushés vers `neomnia/Kubinote-GitOps` |
| 2026-04-27 | **fix(namespaces)** : `open-webui` namespace vide supprimé (doublon de `interfaces`) + retiré de la kustomization pour éviter la recréation automatique ; PVs `dify-*` en état `Released` (claimRef `mindstudio-prod`) récupérés via patch + rebind vers namespace `dify` ; 7 pods Dify Running |
| 2026-04-27 | **Phase 7** : admin-sys promu K8s executor v4.0 — nouveau script FastAPI (`/execute`, `/apply`) remplace `penpot-sidecar:v3.5` ; `ClusterRole admin-sys-executor` (lecture universelle + mutations workloads/config/RBAC/batch) ; Charlotte `_kubectl()` route via `POST admin-sys/execute` : mutations obligatoires via admin-sys, read-only avec fallback local si admin-sys KO ; `ADMIN_SYS_URL` dans charlotte-config |
| 2026-04-27 | **fix(nox/neon)** : `POST /projects` bloqué (org managed by Vercel) → pattern branche-par-projet : Nox crée une branche Neon sur le projet existant `NeoBridge` (`young-fog-76038471`) — `neon_branch_id` + `neon_endpoint_host` dans le résultat ; `NEON_BASE_PROJECT_ID` ajouté dans deployment-nox + configmap ; branche de test supprimée |
| 2026-04-27 | **fix(leon/temporal)** : `TEMPORAL_NAMESPACE` de Leon corrigé `default` → `leon` dans `configmap-leon-config.yaml` (namespace Temporal `leon` créé en Phase 6) |
| 2026-04-27 | **feat(admin-sys/auth)** : token `X-Admin-Sys-Token` ajouté sur `/execute` et `/apply` — secret `admin-sys-token` (64 hex) dans namespaces `interfaces` + `agent-system` ; `/health` reste libre (probes) ; Charlotte injecte le header dans `_kubectl()` et `_kubectl_apply_yaml()` (migré de kubectl local vers admin-sys `/apply`) ; validé 403 sans token, 200 avec token |
| 2026-04-27 | **fix(zoho-connector)** : HTTP methods corrigés — PUT/PATCH/DELETE utilisaient `c.post()` → méthodes httpx correctes ; Zoho erreur 6500 (soft) traitée comme non-fatale (ressource créée côté Zoho malgré HTTP 400) — retourne `{}` au lieu de lever exception |
| 2026-04-27 | **fix(zoho-observer v2.0)** : `_zoho_proxy` — `"body"` → `"data"` (champ attendu par ProxyReq zoho-connector) ; import `timezone` ajouté ; champs `acceptance_criteria` + `emitted_at` ajoutés dans spec pour satisfaire `dispatcher_validate_spec` ; déduplication déléguée à Temporal (`WorkflowIDReusePolicy.ALLOW_DUPLICATE_FAILED_ONLY`) au lieu du set `_dispatched` volatile |
| 2026-04-27 | **fix(dispatcher)** : workflow ID sans timestamp (`devproject-{project_id}`) + `WorkflowIDReusePolicy.ALLOW_DUPLICATE_FAILED_ONLY` + catch `WorkflowAlreadyStartedError` — idempotence garantie même après restart pod zoho-observer |
| 2026-04-27 | **fix(aria/nox)** : `GET /git/refs/heads/main` retourne 409 sur repo template en cours d'init — erreur 409 ignorée silencieusement au lieu d'être ajoutée à `result["errors"]` |
| 2026-04-27 | **fix(vera)** : check `nox_db` corrigé — `neon_project_id` (ancien champ) → `neon_branch_id` (sortie actuelle de Nox) ; même correction dans le prompt LLM |
| 2026-04-27 | **feat(qdrant)** : collection `pm-experience` créée (768 dims, Cosine) — manquante, causait une boucle d'erreur dans le PM observer |
| 2026-04-27 | **E2E pipeline validé** : test complet zoho-observer → Dispatcher → Aria+Nox (parallèle) → Vera (approved) → signal humain → deploy → COMPLETED en 44.86s (workflow `devproject-zoho-2114101000001568047`) ; idempotence vérifiée post-restart pod |
| 2026-04-28 | **Stalwart Mail Server v0.11.8** : déployé dans namespace `stalwart` — StatefulSet (PVC 10Gi local-path), listeners SMTP/SMTPS/Submission/IMAP/IMAPS/Sieve/HTTP tous opérationnels ; ingress `mail.neokube.fr` (Traefik) ; LoadBalancer 192.168.1.28 ports mail ; fix config path (initContainer `/config/config.toml` ≠ `/config/etc/config.toml`), log tracer stdout, probe tcpSocket ; DKIM RSA 2048 selector `mail` stocké dans secret `stalwart-dkim` + Vault `secret/neokube/apps/stalwart` |
| 2026-04-28 | **openprovider-connector v1.0** (port 8005) + **cloudflare-connector v1.0** (port 8006) : déployés et validés — Openprovider : 7 domaines accessibles (neokube.fr géré par Openprovider DNS), JWT re-login auto ; Cloudflare : 19 zones actives (neomnia.net zone_id=8c1283e7c52c34a9d5112c0fb271af27, account_id=822ba0e8c232e192475e6bd02ce36cb4), neokube.fr absent (DNS Openprovider direct) ; credentials dans Vault `secret/neokube/infrastructure/{openprovider,cloudflare}` |
| 2026-04-28 | **Domi v1.0** — Domain Infrastructure Manager (port 8489, domi-queue, namespace dispatcher) : 4ème activité parallèle dans DevProjectWorkflow (gather 3→4) ; mode subdomain : CNAME {slug}.neomnia.net → cname.vercel-dns.com dans Cloudflare zone neomnia.net ; mode register : achat Openprovider + zone Cloudflare + NS update ; domi_link_vercel_domain post-deploy ; DomainRenewalScanWorkflow cron 09:00 UTC (auto-renew <30j, alerte Charlotte <60j) ; Vera v1.2 check domain_provisioned (non-bloquant) ; registre v1.7 |
| 2026-04-28 | **Phase 10d** : `penpot-connector` v1.0 (port 8004, connector-system) — proxy Penpot RPC API, auth session cookie depuis Vault `secret/neokube/infrastructure/penpot` (PENPOT_EMAIL/PASSWORD), re-login auto 401 ; agent `Penpot` v1.0 (port 8488, penpot-queue, agent-system) — activité `penpot_create_design` : create-project + duplicate-file template → retourne `penpot_url` (/design?project-id=X&file-id=Y) ; non-bloquant si PENPOT_TEMPLATE_FILE_ID/PENPOT_TEAM_ID vides ; DevProjectWorkflow : gather 2→3 (Aria+Nox+**Penpot** en parallèle) ; vera_review : 4ème param `penpot_result` (non-bloquant) ; dispatcher_zoho_callback : ligne "Design" ajoutée ; pm-decisions : penpot_url vectorisé ; registre agents v1.6 |
| 2026-04-28 | **fix(pipeline/audit)** : 5 bugs bloquants corrigés — (1) Vercel repoId `str→int` (`incorrect_git_source_info`) + fallback org/repo ; (2) `asyncio.gather return_exceptions=True` Penpot+Domi non-bloquants, Aria+Nox re-raise ; (3) Leon `dispatch_project` : `domain_mode`+`domain_name` ajoutés au schema tool et spec JSON ; (4) `CF_ACCOUNT_ID` ajouté dans `configmap-domi-config` (mode register) ; (5) restart Aria+Nox+Dispatcher+Leon après update configmaps ; section "Pièges connus" ajoutée dans CLAUDE.md |
| 2026-04-28 | **Stalwart — calibration mail** : auto-ban `[server.fail2ban] rate="100/1d"` actif dans config.toml ; domaine `neokube.fr` + compte `admin@neokube.fr` créés via API REST ; DNS zone neokube.fr créée chez Openprovider (A/MX/SPF/DKIM/DMARC) via openprovider-connector (PUT zone — POST/PATCH non implémentés) ; `stalwart-connector` v1.0 (port 8007, connector-system) : auth Basic injectée auto, endpoints `/accounts`, `/accounts/create`, `/accounts/{account}` DELETE, `/proxy` ; Vault `secret/neokube/apps/stalwart` contient `ADMIN_PASSWORD` ; registre agents mis à jour (section connectors) ; section Stalwart gotchas ajoutée dans CLAUDE.md |
| 2026-04-29 | **stalwart-connector déployé** : pod Running dans `connector-system` — GET /accounts, POST /accounts/create, DELETE /accounts/{account} validés ; Vault `ADMIN_PASSWORD` corrigé (`SU0ie4btcEWNmRq7RBb10Z8RimN3V` — correspond au hash `[authentication.fallback-admin]` dans config.toml) ; namespace `stalwart` ajouté dans CLAUDE.md ; merge remote Domi commits (96d616b → 8c68f68) résolu |
| 2026-04-29 | **feat(mail): identités agents** — 4 comptes Stalwart créés : `leon@`, `vera@`, `domi@`, `no-reply@neokube.fr` ; credentials dans Vault `secret/neokube/agents/{leon,vera,domi}` + `apps/stalwart.NOREPLY_PASSWORD` ; STALWART_CONNECTOR_URL + MAIL_FROM + SMTP_HOST/PORT ajoutés dans configmaps Leon/Domi et deployment Vera ; **Dispatcher v1.1** : `dispatcher_send_client_mail` (aiosmtplib, SMTP Stalwart port 587, email HTML/texte post-deploy) ; `client_email` dans ProjectSpec (3 endroits : tool schema Leon + spec dict + validate_spec setdefault) ; step 9 workflow non-bloquant |
| 2026-04-29 | **Penpot Vault** : credentials provisionnés dans Vault `secret/neokube/infrastructure/penpot` (PENPOT_EMAIL, PENPOT_PASSWORD, PENPOT_TEAM_ID=82052e4a-…) ; reset mot de passe Argon2id format custom Penpot (argon2id$hexsalt$m$t$p$hexhash) ; penpot-connector login 200 confirmé ; `PENPOT_TEAM_ID` mis à jour dans `deployment-penpot.yaml` |
| 2026-04-29 | **E2E pipeline complet validé** : `devproject-e2e-test-b468b5b0` — Aria+Nox+Penpot+Domi (parallel) → Vera (approved) → deploy Vercel → pm-decisions → send_client_mail ; DomainRenewalScanWorkflow `domi-renewal-scan-daily` RUNNING dans Temporal |
| 2026-04-29 | **fix(smtp+embed)** : (1) SMTP_HOST corrigé `stalwart-web` → `stalwart-mail` dans tous les agents (port 587 SMTP submission, `stalwart-web` = HTTP admin uniquement) ; (2) `_embed()` fix format HuggingFace — LiteLLM router retourne 768 scalaires séparés dans `data` au lieu d'un vecteur unique ; `data[0]["embedding"]` était un float causant HTTP 400 Qdrant ; fix dans dispatcher + charlotte-sre ; piège documenté dans CLAUDE.md §7 |
| 2026-04-29 | **fix(smtp+dispatch)** : (3) `on_approved/on_rejected` signal handlers : ajout `reason: str = ""` — Temporal passe le payload signal en arg positionnel, `on_approved(self)` levait TypeError ; (4) `validate_certs=False` dans aiosmtplib.send (Stalwart self-signed cert) ; (5) rôle `"user"` ajouté sur 4 comptes Stalwart (leon/vera/domi/no-reply) — sans ce rôle Stalwart retourne 550 5.7.1 ; **pipeline E2E complet validé** : deploy Vercel + Qdrant pm-decisions HTTP 200 + email envoyé à chvandendriessche@neomnia.net |
| 2026-04-29 | **fix(penpot)** : `PENPOT_TEMPLATE_FILE_ID` provisionnée — fichier `template-maquette-base` créé dans Penpot Drafts (id=`32796cdf-d506-81b0-8007-f19045833782`) ; `deployment-penpot.yaml` mis à jour + Vault `secret/neokube/infrastructure/penpot` ; `penpot_url` non-null désormais dans les runs pipeline |
| 2026-04-29 | **fix(dispatcher/charlotte)** : timeout `dispatcher_notify_approval` Charlotte 10s → 60s (LLM call sur `/mission` dépassait le timeout) |
| 2026-04-29 | **cleanup** : 16 repos test GitHub (neomnia org) supprimés + 9 projets Vercel associés supprimés |
| 2026-05-02 | **fix(cluster-bootstrap)** : image `temporalio/temporal:1.30.2` inexistante sur Docker Hub → `latest` ; CronJob débloqué (`concurrencyPolicy: Forbid` bloquait tous les runs suivants) |
| 2026-05-02 | **fix(zoho URLs)** : URLs projets/tâches incorrectes dans Charlotte et Leon (format `#zp/dashboard/{id}` inventé) → URL validée `#zp/projects/{id}/` (SPA fragment) |
| 2026-05-02 | **feat(zoho-connector v1.1)** : `_inject_web_urls()` centralisé — enrichit chaque réponse proxy avec `web_url` pour projets/tâches/milestones/tasklists ; Charlotte + Leon migrent de la construction locale vers `item["web_url"]` ; section "Règles de conception connector-system" + piège §8 ajoutés dans CLAUDE.md |
| 2026-05-02 | **feat(charlotte): `project_health_check`** — bilan cross-systèmes Zoho+GitHub+Vercel+Penpot+Notion en un appel parallèle ; `update_docs=True` croise les liens dans Zoho description + page Notion ; règle 11 dans le prompt (pas de `zoho_list_projects` pour les demandes "vérifier/checker/rassure-moi") |
| 2026-05-02 | **doc: cycle de vie projet** — section "Planification → Production" ajoutée dans CLAUDE.md : 3 phases (Exploration/Planification/Production), frontière de déclenchement annotée dans le diagramme DevProjectWorkflow, 3 gaps documentés (trigger Zoho status P1, mapper Zoho→ProjectSpec P1, email enrichi P3) |
| 2026-05-02 | **fix(dns/neokube.fr)** : DNS zone opérationnelle — (1) bug Openprovider API identifié : `{"zone":{"records":[...]}}` retournait success:true silencieusement sans appliquer les records ; format correct = `{"id":zone_id, "name":"zone", "records":{"add":[...]}}` + TTL min 600s ; (2) domaine délégué vers Cloudflare NS sans zone CF → SERVFAIL ; NS remis sur Openprovider via `PUT /domains/29414839` ; (3) 5 records ajoutés : A mail→45.130.81.100, MX prio=10, SPF, DKIM Stalwart, DMARC ; (4) openprovider-connector v1.1 : endpoints `/dns/records/add` + `/dns/records/remove` ; (5) section Scaleway TEM + Penpot SMTP ajoutées dans CLAUDE.md |
| 2026-05-02 | **feat(stalwart): migration Scaleway + fix AUTH PLAIN** — Stalwart déplacé du StatefulSet K8s vers instance Docker DEV1-S Scaleway fr-par-1 (`51.15.253.114`) ; accès SSH permanent (`id_ed25519_neokube`) ; K8s : StatefulSet+PVC supprimés, Services ClusterIP + Endpoints manuels ajoutés (stalwart-mail + stalwart-web → 51.15.253.114) ; dispatcher configmap : `start_tls=True → False` ; **fix AUTH** : bug Stalwart v0.11.8 — `session.auth.mechanisms` doit être une **string expression** `"[plain, login, oauthbearer]"` (pas un tableau TOML, pas le format conditionnel `[[array]]` — tous deux échouent silencieusement ou crashent à cause du tri alphabétique BTreeMap RocksDB) ; `tls.enable = false` sur listener submission ; DNS A `mail.neokube.fr` mis à jour `45.130.81.100 → 51.15.253.114` ; E2E validé : aiosmtplib `start_tls=False` depuis K8s → Stalwart Scaleway → email reçu |
| 2026-05-03 | **fix(stalwart/relay): smtp-tem-proxy** — Scaleway bloque ports SMTP outbound (25/465/587) depuis instances DEV1-S ; proxy Python `smtp-tem-proxy` (systemd, port 1025) créé sur l'instance : accepte SMTP de Stalwart et relaye via API HTTP TEM Scaleway (HTTPS:443) ; 3 bugs corrigés : (1) Stalwart DNS resolver async bypass `/etc/hosts` → utiliser hostname DNS réel `mail.neokube.fr` comme adresse relay ; (2) Stalwart avorte après EHLO sans STARTTLS même avec `tls.enable=false` → `[queue.outbound.tls] starttls="optional"` ; (3) `MAIL FROM:<email> SIZE=...` → `.strip("<>")` laissait `email>` → fix regex `re.search(r'<([^>]+)>', cmd)` ; fix(penpot) SMTP_TLS=false + JAVA_TOOL_OPTIONS vide ; fix(ingress) stalwart-web → `mail-admin.neokube.local` ; E2E validé : Penpot recovery mail reçu |
| 2026-05-03 | **fix(hosts): mail-admin.neokube.local** — ajout `192.168.1.28 mail-admin.neokube.local` dans `/etc/hosts` neokube-beta (manquant → page blanche dans le navigateur) ; à ajouter aussi sur la machine client |
| 2026-05-03 | **fix(stalwart/webadmin): version épinglée v0.1.23** — binaire v0.11.8 embarque un webadmin nécessitant Stalwart ≥ 0.13.0 ("Unsupported server version") ; pinglage sur webadmin v0.1.23 (dernier compatible v0.11.8) via `webadmin.resource` + `webadmin.auto-update = false` + `webadmin.path = /opt/stalwart-mail/etc/webadmin` dans config.toml ; login webadmin opérationnel |
| 2026-05-03 | **feat(stalwart): Roundcube webmail** — déployé dans namespace `stalwart` (`roundcubemail:latest-apache`) ; IMAP → stalwart-mail:143, SMTP → stalwart-mail:587 (credentials `%u`/`%p`) ; sqlite PVC 1Gi local-path ; ingress `webmail.neokube.local` (Traefik) ; `/etc/hosts` neokube-beta mis à jour ; opérationnel HTTP 200 |
| 2026-05-03 | **feat(infra): Cloudflare Tunnel** — `neokube-tunnel` (ID `94ff6f9f-2498-470e-9a7b-b4d3ed9e94fb`) ; `cloudflared:latest` 2 replicas dans kube-system ; 8 connexions QUIC actives vers datacenter CF Paris ; config ingress : 8 services (chat/headlamp/temporal/langfuse/webmail/mailhub/design/dify) → Traefik Host override ; GitOps `apps/cloudflare-tunnel/base/` |
| 2026-05-03 | **feat(cloudflare): two-token architecture + CNAMEs** — séparation `Neomnia-account` (CF_API_TOKEN, compte complet) / `Neomnia-domains` (CF_DNS_TOKEN, Zone DNS:Edit all zones) ; cloudflare-connector v1.1 : `CF_DNS_TOKEN` prioritaire, fallback `CF_API_TOKEN` ; Vault `secret/neokube/infrastructure/cloudflare` version 4 (CF_DNS_TOKEN ajouté) ; 8 CNAMEs neomnia.net créés (chat/headlamp/temporal/langfuse/webmail/mailhub/design/dify → tunnel) ; tunnel opérationnel : HTTP 200 vérifié sur chat.neomnia.net + headlamp.neomnia.net |
| 2026-05-03 | **feat(surfsense): déploiement SurfSense v1.0** — moteur RAG open-source (alternatif Perplexity) dans namespace `surfsense` ; 7 composants K8s : postgres (pgvector/pg17, 10Gi, wal_level=logical), redis, searxng, backend FastAPI, celery worker, zero-cache (rocicorp/zero:0.26.2, 1.5Gi RAM), frontend Next.js ; embedding `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` local 384-dim (chonkie v1.6 ignore base_url openai → pas de LiteLLM pour les embeddings) ; Langfuse (LangSmith-compat), Notion OAuth ; 3 ingresses dual-host (neokube.local + neomnia.net) ; 3 CNAMEs + 3 règles tunnel Cloudflare ajoutées (surfsense/surfsense-api/surfsense-zero) ; HTTP 200 validé sur surfsense.neokube.fr + surfsense-api.neokube.fr/health |
| 2026-05-03 | **fix(litellm): OOMKill** — memory limit 512Mi → 2Gi ; LiteLLM crashait en boucle (exit 137) depuis 8j (7 restarts), causait l'échec de tous les appels LLM SurfSense et autres agents ; fix GitOps `deployment-litellm.yaml` |
| 2026-05-03 | **fix(surfsense): LLM preferences + prompts** — l'espace "Neomnia Studio" était configuré sur AUTO (mode cloud SurfSense) au lieu de GPT-4o via LiteLLM ; switché sur `agent_llm_id=1` (GPT-4o) via `PUT /api/v1/search-spaces/1/llm-preferences` ; 7 configs LLM créées (GPT-4o, Mistral, Codestral via API + Gemini Flash/Pro/Claude Sonnet/Opus via INSERT SQL direct car validation SurfSense échoue sur quota/billing) ; prompts E2E validés |
| 2026-05-03 | **doc: architecture domaines + process CF** — section "Architecture des noms de domaine" ajoutée dans CLAUDE.md : règle fondamentale neomnia.net=public/neokube.local=LAN/neokube.fr=mail, process complet "ajouter un service public", anti-pattern CNAME Openprovider→cfargotunnel sans proxy CF, règle R5 connector-system |
| 2026-05-03 | **feat(scraping): google-discovery-connector v1.0 + crawlee-service v1.0** — deux microservices scraping dans `connector-system` ; `google-discovery-connector` (port 8008, Python FastAPI) : `POST /search` → Google Custom Search API, credentials Vault `secret/neokube/infrastructure/google` (GOOGLE_SEARCH_API_KEY + GOOGLE_CX_ID), 100 req/j gratuites ; `crawlee-service` (port 8009, Node.js, image `mcr.microsoft.com/playwright:v1.49.0-noble`) : `POST /crawl`, `POST /batch` (max 10 URLs, concurrence 3), `POST /screenshot` → PNG base64, mutex interne pour sérialiser les sessions Chromium ; startup ~2min (npm install crawlee@3 + playwright install chromium) ; GitOps `apps/connector-system/base/` mis à jour |
| 2026-05-03 | **feat(infra): migration neokube.fr → Cloudflare + domaine de référence** — zone CF `neokube.fr` créée via CF_GLOBAL_KEY (`c58eafb1b5550...`, `informatique@neomnia.net`) ; NS Openprovider → `abby.ns.cloudflare.com`/`david.ns.cloudflare.com` (propagation immédiate) ; records mail recréés dans CF (A/MX/SPF/DKIM/DMARC, `mail.neokube.fr` DNS-only) ; 11 CNAMEs tunnel créés dans zone neokube.fr ; tunnel rules reset vers neokube.fr uniquement (11 règles propres) ; `neokube.fr` devient le domaine de référence NeoKube (remplace neomnia.net pour tous les nouveaux services) ; Vault `CF_GLOBAL_KEY`+`CF_ACCOUNT_EMAIL`+`CF_NEOKUBE_ZONE_ID`+`CF_NEOKUBE_DOMAIN` provisionnés |
| 2026-05-03 | **fix(cloudflare-connector v1.2): Global API Key prioritaire** — cloudflare-connector v1.2 : priorité d'auth Global Key (`X-Auth-Email`+`X-Auth-Key`) > CF_DNS_TOKEN > CF_API_TOKEN ; résout l'impossibilité de créer des zones CF avec les tokens scoped (permission `zone.create` non accordable sur tokens `cfat_`) |
| 2026-05-03 | **fix(surfsense): CORS + migration neokube.fr** — root cause "Unable to connect to the server" : frontend `http://surfsense.neokube.local` appelait `https://surfsense-api.neomnia.net` → CORS 400 (origin non whitelistée) ; fix : `NEXT_FRONTEND_URL=https://surfsense.neokube.fr` dans configmap, 3 ingresses mis à jour (neomnia.net → neokube.fr), Domi configmap domain_ref migré, dispatcher fallback migré ; HTTP 200 sur surfsense.neokube.fr |
| 2026-05-03 | **feat(surfsense): connecteur Notion** — token legacy `[NOTION_TOKEN_REDACTED]` ; connecteur inséré directement en DB (id=1, space=1, `NOTION_INTEGRATION_TOKEN`) ; 444 pages Notion accessibles via l'intégration ; indexation déclenchée via Celery `index_notion_pages` sur queue `surfsense.connectors` ; `enable_summary=false` pour éviter 444 appels LLM ; indexation périodique toutes les 24h activée |
| 2026-05-04 | **feat(penpot): v3.0 — Zoho + Notion + wireframes multi-pages** — agent Penpot lit les tâches/jalons Zoho (via Vault creds + refresh token direct), lit la doc Notion (token `ntn_...` dans Vault `secret/neokube/apps/notion`), poste commentaire Zoho sur les tâches design après création (HTTP 201 validé) ; wireframes v3 : une frame Penpot par page du site extraite des tâches Zoho (Home/Services/À propos/Contact) + layouts spécifiques + frame jalons ; deployment-penpot mis à jour (VAULT_ADDR, VAULT_TOKEN, ZOHO_PORTAL_ID) ; fix Vault KV v2 path (`secret/neokube/...` → `/v1/secret/data/neokube/...`) |
| 2026-05-04 | **feat(spec): ProjectSpec 13 champs — ajout `notion_page_id`** — `notion_page_id` ajouté aux 3 endroits (règle anti-pattern 3) : schema JSON Schema Leon (`dispatch_project`), dict spec `_execute_tool`, `dispatcher_validate_spec` setdefault ; optionnel (None par défaut, option 2 validée) ; si présent → Penpot agent charge la page Notion avant de générer les wireframes |
| 2026-05-04 | **feat(penpot): v3.3 — wireframes dual desktop+mobile 9 pages + RAG** — frames desktop 1440px + mobile 375px côte à côte ; hauteurs calculées au contenu (DH/MH exacts) ; footer enrichi (adresse Paris, contact@neomnia.studio) ; _verify_penpot_url (get-project-files) ; brief design complet sur les tâches Zoho (statut/avancement/priorité/durée) ; indexation RAG 10 points dans collection `zoho-tasks` (1 global + 9 pages) ; auth LiteLLM LITELLM_API_KEY dans deployment-penpot ; brand color #32AFB1 extrait de neomnia.net |
| 2026-05-04 | **feat(connector): dataforseo-connector v1.0** — port 8010, connector-system ; credentials Vault `secret/neokube/infrastructure/dataforseo` (DATAFORSEO_LOGIN/PASSWORD/API_KEY) ; `POST /search` fallback auto DataForSEO → SearXNG (surfsense-searxng self-hosted) ; `POST /proxy` accès direct DataForSEO v3 ; E2E validé provider=dataforseo 5 résultats Penpot SaaS UI kit |
| 2026-05-04 | **fix(penpot): PENPOT_FRONTEND_URL → design.neokube.fr** — URL de livraison corrigée : `http://penpot.neokube.local` → `https://design.neokube.fr` (URL publique Cloudflare Tunnel) ; liens Zoho/email/pm-decisions pointent désormais vers l'URL publique accessible |
| 2026-05-04 | **doc(penpot): gestion projets/fichiers** — section "Penpot — Gestion des projets" ajoutée dans CLAUDE.md : soft-delete (deleted_at+7j visible dans get-projects), hard-delete SQL procédure complète (tables NO ACTION vs CASCADE), bug 204 connecteur cosmétique, URL livraison, vérification fichier, idempotence, RAG zoho-tasks |
| 2026-05-04 | **cleanup(penpot): purge 10 anciens projets** — 9 doublons "Neomnia.net Refonte — Design" + 1 "Neomnia.net — Refonte" (Default team) supprimés via SQL hard-delete ; Penpot.neokube.local ne retourne plus que 3 projets : 2 Drafts + 1 actif |
| 2026-05-04 | **feat(penpot): wireframes v4 — design system v2 + composants nav 4 états + hero dashboard** — `_img_placeholder()` helper remplace les rectangles gris plats (crosshair + badge PRIMARY + dimensions) ; hero desktop : dark shell UI avec topbar, 4 KPI cards, 7-bar chart, agent status sidebar ; hero mobile : compact dark dashboard ; `_build_design_system()` v2 : 5 sections (colors usage, typo 6 variants, grid+spacing, motion tokens, image variants) ; `_build_components()` v2 : 4 sections (6 boutons×2états, nav 4 états+annotations transition, 7 badges, 4 états formulaire) |
| 2026-05-04 | **feat(indexer): design-knowledge-indexer v1.0** — CronJob dimanche 4h Europe/Paris, namespace management ; pipeline DataForSEO SERP → crawlee-service scraping → LiteLLM embed → Qdrant collection design-knowledge (768 dims, Cosine) ; 17 requêtes / 9 catégories + 10 URLs statiques ; enrichissement agent penpot (_query_design_knowledge + _build_inspiration_frame à Y=18000) ; fix crawlee-service : playwright@1.49.0 explicite + PLAYWRIGHT_BROWSERS_PATH=/ms-playwright (MCR image pré-installé, startup 60s vs 3min) ; secret cluster-manager-secrets (LITELLM_MASTER_KEY=sk-neokube-litellm-master) à créer dans namespace management au bootstrap |
| 2026-05-04 | **feat(indexer): crawlee-service v2 — réécriture Playwright direct** — remplacement de la couche Crawlee par Playwright direct (`chromium.launch()` + pool de 2 pages partagées) ; root cause : Crawlee cacheait `CRAWLEE_STORAGE_DIR` à l'import (singleton Configuration) ignorant les modifications runtime ; bug AutoscaledPool : sous-lecture cgroup (768MB au lieu de 3Gi) → throttle total ; résultats : startup ~60s, `/crawl`, `/batch`, `/screenshot` opérationnels ; memory limit 512Mi → 3Gi ; 53 points indexés dans `design-knowledge` |
| 2026-05-04 | **feat(indexer): penpot-template-indexer v1.0** — crawl Penpot Hub (libraries-templates + 7 URLs catégorie + community) ; classification 15 types de projet (wireframe/icon-set/design-system/ui-kit/dashboard/landing-page/mobile-app/ecommerce/blog/portfolio/saas-webapp/planning/ux-research/presentation/framework-kit) ; 226 templates indexés dans Qdrant collection `penpot-templates` (768 dims, Cosine) ; CronJob annuel (manuel) ; gotcha : LITELLM_URL doit inclure `:4000` (port non-standard) |
| 2026-05-04 | **feat(surfsense): SharePoint upload** — script `~/scripts/sharepoint_to_surfsense.py` ; upload 1 995 fichiers depuis `~/SharePoint/` (8 sites prioritaires) via `POST /api/v1/documents/fileupload` ; filtres venv/~$ ; JWT token dans `SURFSENSE_URL=http://surfsense-api.neokube.local` ; connecteur OneDrive personnel créé (ID 2, `chvandendriessche@neomnia.net`) via Azure App Registration `SurfSense-Neokube` (CLIENT_ID dans Vault `secret/neokube/apps/surfsense`) |
| 2026-05-04 | **feat(monitoring): Grafana + Loki + Promtail** — namespace `monitoring` ; Loki 2.9.8 (log aggregation, 30j rétention, hostPath `/var/lib/loki` 10Gi) ; Promtail 2.9.8 (DaemonSet, collecte tous les pods via `/var/log/pods`, labels namespace/app/pod/container/job_name) ; Grafana 10.4.3 (dashboard pré-provisionné "NeoKube — CronJobs & Batch" : 4 panels CronJobs/RAG/erreurs/agents) ; Vault `secret/neokube/apps/monitoring` (GRAFANA_ADMIN_PASSWORD) ; `grafana.neokube.local` + `grafana.neokube.fr` (CNAME + règle tunnel CF) ; fix inotify : `fs.inotify.max_user_instances=512` dans `/etc/sysctl.d/99-inotify.conf` |
| 2026-05-04 | **feat(monitoring/smtp): smtp-relay pod** — Go's smtp.PlainAuth refuse les credentials en clair sur connexion non-TLS (hostname ≠ localhost) → déploiement d'un relay Python (aiosmtpd+aiosmtplib) dans namespace monitoring ; Grafana → smtp-relay:25 (plaintext, sans auth) → stalwart-mail:587 (avec credentials no-reply) → TEM → chvandendriessche@neomnia.net ; 3 règles d'alerte Loki provisionnées : CronJob ERROR, backup TERMINÉ, indexer TERMINÉ ; **gotcha : `admin@neokube.fr` ne peut pas recevoir de mails** — conflit avec `authentication.fallback-admin.user="admin"` dans config.toml Stalwart (SMTP lookup du local-part "admin" trouve le superuser système, pas le compte individual) ; utiliser `chvandendriessche@neomnia.net` pour les alertes monitoring |
| 2026-05-05 | **fix(surfsense): celery OOMKill + DB migrations** — surfsense-celery crash 128 restarts (exit 137) : autoscale max=10 workers × 460MB sentence-transformers = OOMKill ; memory limit 4Gi → 8Gi ; migrations Alembic désynchronisées (alembic_version=116, schema=~140) : table agent_action_log hors Alembic bloquait migration 130 — renamed + DROP CASCADE index orphelins, stamp 140, alembic upgrade head applique 141+142 ; SurfSense pleinement fonctionnel |
| 2026-05-05 | **fix(surfsense): JWT + dédup + LLM quota** — token expiré (2026-05-04 20:50 UTC) → nouveau token PyJWT exp 2027-05-05 ; `GET /documents/search/titles` format changé (list → `{items, has_more}`) → fix parse + page_size=50000 ; tous les LLM API quotas épuisés simultanément (OpenAI/Anthropic/Gemini) → mistral-large-latest comme provider de secours ; 2 629 fichiers SharePoint (9 sites dont Management) re-indexés |
| 2026-05-05 | **feat(alerting): ntfy v2.11.0 + système de notifications push** — ntfy déployé dans namespace `interfaces` (NTFY_AUTH_DEFAULT_ACCESS=deny-all, upstream ntfy.sh pour relay iOS/Android push) ; CNAME `ntfy.neokube.fr` + règle tunnel Cloudflare ; Vault `secret/neokube/apps/ntfy` (NTFY_ADMIN_PASSWORD + NTFY_AGENT_PASSWORD) ; `llm-key-validation` v2 : schedule 6h→*/6h, vrais appels complétion 1 token (détecte quota épuisé), notification ntfy priorité urgent/high selon providers KO ; Grafana : contact point ntfy-neokube webhook (POST auth Basic, routes severity=error\|warning) ; Charlotte SRE : activity `sre_ntfy_notify` — OOMKill/CrashLoop/severity critical|warning → urgent/high ; Dispatcher : activity `dispatcher_ntfy_notify` — blocked_vera/approval_timeout/deploy_failed/deployed ; helper `_ntfy_notify` + NTFY_URL/NTFY_USER/VAULT_NTFY_PATH ajoutés dans configs Charlotte et Dispatcher |
| 2026-05-06 | **feat(eval): Langfuse data + evaluation agents** — LangFuse 0 traces → traces opérationnelles (root cause: LANGFUSE_PUBLIC_KEY passé en dur ignorant secretKeyRef) ; fix deployment Charlotte + Penpot (secretKeyRef cluster-manager-secrets) ; `llm-key-sync` déclenché pour regénérer les virtual keys LiteLLM correctes ; activation PostgreSQL LiteLLM (R9.5 ✅) |
| 2026-05-06 | **fix(agents): LLM fallback — quota Anthropic/Gemini épuisés** — Charlotte + Penpot → mistral (Anthropic quota épuisé) ; Dispatcher + Domi → mistral (Gemini-flash quota épuisé) ; CLAUDE.md R9 mis à jour avec modèles actuel vs cible + procédures restore |
| 2026-05-06 | **feat(eval): agent_eval.py v1.0** — LLM-as-judge pour 9 agents NeoKube (3 scénarios chacun) ; scores relevance/quality/instructions (0-10) ; post `eval_avg` Langfuse + résumé terminal ; `_kubectl_secret()` pattern anti-hardcode (R13) |
| 2026-05-06 | **feat(rag): reindex_neo_knowledge.py** — script ré-indexation CLAUDE.md → Qdrant `neokube-process-docs` (768 dims, 70→77 chunks) ; `paraphrase-multilingual-mpnet-base-v2` via HuggingFace Inference API ; suppr anciens points avant ré-indexation |
| 2026-05-06 | **feat(langfuse): prompts Dispatcher + Nox enregistrés** — `dispatcher-orchestrator` v1 : return_exceptions=True, Penpot/Domi non-bloquants, 9 étapes workflow ; `nox-backend` v1 : contrainte NeoBridge explicit (young-fog-76038471, POST /projects bloqué) ; scores après fix : Dispatcher 10.0/10 (was 7.6), Nox 10.0/10 (NeoBridge scenario) |
| 2026-05-06 | **feat(neo): system prompt v2 — URLs NeoKube de référence** — configmap + Langfuse `neo-assistant` v2 : section "INFRASTRUCTURE NEOKUBE — URLS DE REFERENCE" (11 services publics neokube.fr + 8 services internes neokube.local) ; règle 8 anti-invention d'URLs ; score Neo : 7.7 → 9.0/10 (+URL scenario) |
| 2026-05-06 | **feat(eval): agent-eval-nightly CronJob + Charlotte sre_check_eval_scores** — CronJob `agent-eval-nightly` dans agent-system (2h Paris, script inline `agent_eval_cron.py`) ; 9 agents × 3 scénarios opérationnels ; ntfy si avg < 7.5 ; Charlotte `sre_check_eval_scores` (Bloc E) : toutes les 6 scans → Langfuse rolling avg → ntfy + llm-key-sync si dégradé ; `sre_ntfy_notify` ajouté à la liste des activités worker (bug latent) |
| 2026-05-06 | **fix(charlotte): namespaces corrects v2 + system prompt R9/Bloc E** — 4 erreurs v1 corrigées : `agent-ops`→`agent-system` (Leon), `open-webui`→`interfaces` (admin-sys/ntfy), `vault`→`security` (Vault) ; `_NEOKUBE_AGENT_NAMESPACES` mis à jour (interfaces, connector-system, management) ; system prompt v2 : 13 namespaces réels, section ADMIN-SYS OBLIGATOIRE, R9 fallbacks, Bloc E comportement ; Langfuse `charlotte-sre` v2 enregistré ; RAG neokube-process-docs ré-indexé 70→77 chunks ; éval manuelle 6 scénarios : 9.17/10 (was 6.3/10 CronJob) |

| 2026-05-07 | **feat(charlotte): outil atomique apply_gitops_fix — git_push jamais oublié** — incident surfsense-zero-cache : crash serveur neokube-beta entre `kubectl apply` et `git_push` → pod live fixé mais repo pas à jour → cluster-bootstrap allait reverter ; root cause architecturale : 5 outils séparés (write+apply+verify+push) que le LLM peut s'arrêter d'exécuter à n'importe quelle étape ; solution : `apply_gitops_fix(path, content, deployment, namespace, commit_message)` — wrapper transactionnel qui fait les 4 étapes en une seule opération atomique, rollback auto si apply ou verify échoue, 3 retries+backoff sur git_push, alerte ÉTAT DÉGRADÉ si push impossible ; system prompt RÈGLE GITOPS DRIFT mis à jour (apply_gitops_fix = méthode préférée, procédure 5 étapes = fallback) ; gotchas découverts : ConfigMap sre-script trop grand pour `kubectl apply` → utiliser `kubectl replace` ; label `app=agent-charlotte` ne sélectionne aucun pod → `kubectl get pods -n agent-system \| grep charlotte` ; fix surfsense zero-cache (10Gi heap V8 + limits 12Gi, commit af3db45) définitivement persisté ; commit a5e051b (Charlotte) + af3db45 (surfsense) pushés, Charlotte 3/3 Running |

| 2026-05-13 | **feat(infra): Phase 1 — image neokube-agent-base + migration deployments** — Dockerfile + requirements.txt créés dans `docker/agent-base/` ; GitHub Actions `build-agent-base.yml` avec setup-buildx@v3 (fix cache GHA) ; image 150MB buildée sur GHCR (run #25826350342) ; `ghcr-pull-secret` K8s + CronJob `registry-sync` (nightly renewal depuis Vault) ; migration script appliqué sur 6 deployments (Charlotte, Aria, Nox, Leon, Vera, Penpot) : suppression initContainers pip install, image→agent-base, readiness 60s→15s, liveness 120s→30s ; Charlotte : `install-deps` supprimé (git dans image base), `git-clone` conservé (clone GitOps nécessaire), apt-get retiré des commands ; restart time : 60-90s → ~15s (hors git-clone ~20s pour Charlotte) |

| 2026-05-13 | **feat(charlotte): Phase 2 — compression Mistral contexte ReAct** — `_compress_tool_result(tool_name, tool_result, user_query)` : si `run_kubectl`/`read_file` > 1500 chars → appel Mistral (`LLM_SCAN_MODEL`) qui extrait uniquement les anomalies en < 400 chars ; remplace troncature brute `[:2500]` qui pouvait perdre les Events en fin de `kubectl describe` ; fallback troncature 2500 si Mistral échoue ; antipattern #38 documenté |

| 2026-05-13 | **feat(agents): Phase 3 — Leon endpoint /sre-task + Charlotte delegate_sre_task** — séparation SRE/PM : Charlotte diagnostique, Leon écrit dans Zoho ; `POST leon:8181/sre-task` {title, description, priority, project_id} → génère REF-YYYYMMDD-XXX + dates auto + tags charlotte/sre + crée tâche Zoho ; Charlotte : tool `delegate_sre_task` + LEON_URL + règle 1c mise à jour (fallback: zoho_create_task si Leon indisponible) ; testé live HTTP 200 id=2114101000001692003 |

| 2026-05-13 | **feat(agents): Phase 4 — observabilité + résilience agents pipeline** — Aria v3.1 + Nox v3.1 : `_ntfy()` + `_langfuse_trace()` en fin d'activité, NTFY_AGENT_PASSWORD via secret `ntfy-agent-creds` ; Vera v1.1 : score QA numérique `qa_score` 0-10 basé checklist + `_langfuse_score()` (trace + score-create Langfuse) + ntfy APPROVED/REJECTED ; Penpot v3.1 : `_penpot()` et `_update_file()` avec retry exponentiel 3× sur 5xx/timeout ; Domi v1.1 : ntfy domain-provisioned |

| 2026-05-16 | **feat(leon): REVIEW workflow déterministe v3.1** — refactor complet : LLM génère le spec (texte pur), Python écrit dans Notion (notion_update_page) sans décision LLM (anti-L8) ; 5 labels intent dont `review` déclenché par URL Notion dans l'historique ; cascade LLM R9 : gpt-4o (TASK) → claude-sonnet (REVIEW) → mistral (fallback) ; sk-leon allowlist complète LiteLLM ; antipattern anti-hallucination "je ne peux pas accéder à Notion" supprimé |

| 2026-05-16 | **feat(leon): RAG leon-memory + rag_mission** — `qdrant_search_leon` sur collection `leon-memory` (expériences PM) ; auto-apprentissage REVIEW : `_extract_and_index_lessons()` en arrière-plan post-session ; `rag_mission` : orchestration RAG depuis brief Notion → Qdrant index Milo ; streaming progress indicators OWU ; notifications ntfy+mail fin mission ; fix notion_read_page : blocs enfants + pagination + sous-pages 2 niveaux |

| 2026-05-16 | **feat(milo+crawlee): pipeline RAG complet** — Milo : endpoint `/scrape-and-index` (scraper + embed + Qdrant) ; crawlee-service v2 : POST /crawl (Playwright direct) ; 4 bugs pipeline RAG corrigés (inference collection, JSON parse, URLs internes filtrées, auto-référence Notion) ; Leon → Milo delegate via `_delegate()` helper HTTP |

| 2026-05-18 | **fix(leon): notion_read_page pagination complète** — pagination has_more + next_cursor (max 1000 blocs) ; child_page blocks : lecture contenu sous-pages (2 niveaux récursifs) ; résumé Mistral étendu 400 mots / 8000 chars input ; block_count retourné pour diagnostic |

| 2026-05-19 | **feat(infra): stack voix complète CLASS A** — whisper-server (faster-whisper base, CPU int8, port 8394) ; media-gateway (préprocesseur multimodal CLASS A port 8395 : Pixtral OCR + Whisper proxy + TTS proxy Mistral `/v1/audio/speech`) ; voice-gateway WebSocket (push-to-talk, port 8393, `wss://voice.neokube.fr/ws`) ; OWU : `STT_ENGINE=openai` → whisper-server, `TTS_ENGINE=openai` → media-gateway ; VOICE_MODE valve bool dans toutes pipes CLASS A ; HTTPS obligatoire `https://chat.neokube.fr` pour getUserMedia |

| 2026-05-19 | **feat(charlotte): Vault agent injection + Scaleway security** — SCW_SECRET_KEY + SCW_ORG_ID injectés par Vault agent depuis `secret/neokube/infrastructure/scaleway` ; rôle `charlotte` + policy `charlotte-policy` Vault ; FallbackModel interactif : claude-sonnet primary pour tool calling (anti-pattern #44) ; guard secret leak : redaction base64 dans run_kubectl get secret ; tools `scw_org_id()` + `scaleway_api` (IAM) + `patch_k8s_secret` ; RÈGLE CRITIQUE étendue : Scaleway billing/IAM explicitement in-scope |

| 2026-05-19 | **feat(monitoring): Scaleway billing NET** — scaleway_billing_total_euros Prometheus (brut − crédit) ; mai 2026 : brut 2878€, crédit −2878€, net 0€ ; dashboard Grafana `scaleway-pilot` ; ConfigMaps `scaleway-billing-history` + `scaleway-inventory-snapshot` dans namespace management ; audit sécurité IP 185.161.169.42/49 (hacking mai 2026) |

| 2026-05-19 | **feat(owu): branding NeoKube + indicateur attente** — logo + titre "NeoKube Intelligence" via ConfigMap ; dots animés fetch-intercept pendant attente réponse agent |

| 2026-05-20 | **feat(neo): v1.4→v1.9 — CDC + Zoho + Pattern B + R9** — v1.4 : outils Zoho create_project/milestone/tasklist/task + confirmation gate ; v1.5 : gpt-4o (ERREUR — clé non autorisée → reverté) ; v1.7 : R9.7 Langfuse metadata + R9.8 fallback HTTP ; v1.8 : routing confirmation + URL Zoho anti-pattern #8 ; v1.9 : classificateur LLM 4 classes anti-pattern #40 (greeting/question/task/confirm). **Note** : CDC + Zoho orchestration appartiennent à Leon (Chef de Production) — Neo = assistant personnel |

| 2026-05-20 | **feat(leon): zoho_create_project + create_milestone + create_tasklist** — 3 activités Temporal + 3 cas LeonZohoWorkflow + 3 tool defs + 3 handlers + routing map + ÉTAPE 5 REVIEW scaffold Zoho complet ; ÉTAPE 5 : zoho_list_projects → create_project → create_milestone × N → create_tasklist → create_task → dispatch_project (webapp) → URL depuis API |

| 2026-05-20 | **refactor(neo): v2.0 — assistant personnel pur** — retrait zoho_create_project/milestone/tasklist (outils + handlers + ~180 lignes system prompt Chef de Projet) ; system prompt : MÉTHODOLOGIE CDC / QUESTIONNAIRE CLARIFYING / TEMPLATE / NORMES TECHNIQUES / ORCHESTRATION ZOHO supprimés ; ajout PÉRIMÈTRE NEO : "CDC+Zoho → Leon, SRE → Charlotte" ; fix chemins Zoho : portal/neomniadotnet double-préfixé → /projects/ (cause root du HTTP 400 bloquant) |

| 2026-05-20 | **fix(leon): LLM_SECONDARY claude-sonnet → gpt-4o** — crédits Anthropic épuisés ; claude-sonnet retourne 400 "credit balance too low" sur toute la génération REVIEW (ProjectSpec + spec final + synthèse) ; configmap-leon-config.yaml LLM_SECONDARY="gpt-4o" ; CLAUDE.md R9 table + agent table mis à jour ; Charlotte non impactée (FallbackModel gère la cascade automatiquement) |

| 2026-05-20 | **fix(leon): R9.8 cascade LLM_SECONDARY — claude-sonnet → gpt-4o automatique** — 5 appels directs `llm.chat.completions.create(model=LLM_SECONDARY)` sans fallback dans le path REVIEW → crash sur quota Anthropic ; helper `_llm_secondary(messages, max_tokens, temperature, timeout)` ajouté : essaie claude-sonnet, cascade vers gpt-4o sur 402/credit/quota, ntfy rate-limitée 1h/modèle ; `LLM_SECONDARY` restauré à "claude-sonnet" (crédits rechargés) ; CLAUDE.md R9 table mis à jour |

| 2026-05-20 | **feat(litellm): router_settings fallbacks trans-agentiques — R9.8 centralisé** — au lieu de réimplémenter la cascade dans chaque agent : LiteLLM router gère le fallback de manière transparente pour tous ; claude-sonnet→gpt-4o→mistral / claude-opus→gpt-4o→mistral / gpt-4o→mistral / gemini-pro→gpt-4o→mistral / gemini-flash→mistral / codestral→mistral / mistral-large-2407→mistral→gpt-4o ; num_retries=2 retry_after=5s ; les helpers _llm_secondary() dans Leon sont désormais redondants (LiteLLM gère avant) mais conservés comme double filet |

| 2026-05-20 | **fix(vault): token Leon expiré — régénération + règle TTL** — Vault retournait 403 sur `secret/neokube/apps/notion` depuis 10:09:26 (token `leon-vault-writer` expiré) ; nouveau token généré avec `leon-policy` TTL=8760h (1 an) ; K8s secret patché sans exposition ; Leon redémarré. Règle : tous les tokens Vault agents doivent avoir TTL=8760h minimum. À vérifier : charlotte-policy, autres agents. |

| 2026-05-20 | **fix(leon): vault-agent-injector — fin des tokens manuels expirants** — rôle Vault `leon` corrigé (était admin-sys/agent-ops → leon-sa/agent-system) ; vault.hashicorp.com/agent-inject activé (était "false") ; NOTION_TOKEN + SURFSENSE_PASSWORD injectés dans /vault/secrets/config.env au démarrage ; startup command source le fichier automatiquement ; vault-agent-init confirmed "rendered config.env" ; plus aucun 403 Vault. Pattern trans-agentique : tous les agents doivent utiliser vault-agent-injector, jamais de token manuel dans un K8s secret. |

| 2026-05-20 | **fix(leon): `_md_to_notion_blocks` robuste — fin du 400 validation_error sur ProjectSpec** — Notion PATCH /blocks/{id}/children + POST /pages renvoyaient 400 sur tout spec contenant tables/checkboxes/bold (cause : blocs `rich_text` vides + markdown non parsé + dépassement 100 children/requête). Fix : `_md_clean()` strip `**`/`*`/`` ` ``/links ; helpers retournent `None` si vide ; chunking 90/requête dans `notion_update_page` + `notion_create_page` ; `- [ ]` → `to_do` natif ; tables Markdown converties en paragraphes propres ; séparateurs `\|---\|---\|` skip ; log body complet + premier bloc sur 4xx. Anti-pattern #46 documenté. **Test E2E demo-rh validé** : spec claude-sonnet 38.2s + Notion write 2.0s, ProjectSpec écrit avec gaps précis (Clerk vs JWT, next-intl, Penpot, DNS demo-rh.neomnia.net), 5 milestones cochables, lien Notion live. |

| 2026-05-20 | **feat(leon): dispatch Zoho + OWU Action button + dispatch_ui** — `POST /dispatch_zoho` : parsing spec via LLM json_object + create_project + create_milestone + create_tasklist + create_task ; OWU Action function `dispatch_zoho_action.py` (toolbar) installée dans webui.db ; `_dispatch_button()` heading-link `## 🚀 [▶ LANCER LE DISPATCH ZOHO ◀](url)` dans les réponses REVIEW ; `GET /dispatch_ui` page HTML standalone (bouton géant amber/rouge + progress JS) ; `POST /dispatch_zoho_from_notion` lit Notion → dispatch. **Abandonnée** : approche bouton/page fragile (OWU n'est pas conçu pour les interactions formulaire). |

| 2026-05-20 | **refactor(leon): anti-pattern #47 — R-TAR trans-agentique — auto-dispatch Zoho après REVIEW** — Principe : agents agissent et rapportent, pas de confirmation UI. Fix : `_dispatch_button` + `/dispatch_ui` + `/dispatch_zoho_from_notion` supprimés ; remplacés par `_find_zoho_project(title)` (matching sémantique alphanum sur projets Zoho actifs) + `_zoho_sync(spec, notion_url, title)` (trouve ou crée, retourne ligne Markdown avec URL). Stream path : yield "> ⚡ Zoho — sync..." pendant l'appel async, lien dans `_fc`. Non-stream path : `await _zoho_sync()` inline. Résultat dans réponse REVIEW : `🔗 Projet Zoho existant : [titre](url)` ou `🚀 Projet Zoho créé : [titre](url) — N milestones · N tâches`. Anti-pattern #47 + règle R-TAR documentés dans CLAUDE.md + CLAUDE-leon.md. |
