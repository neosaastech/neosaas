import { Card, CardHeader, CardTitle } from '@/components/ui/card';

export function WireframesHeader() {
  return (
    <Card className="mb-6">
      <CardHeader>
        <CardTitle>Wireframes Management</CardTitle>
      </CardHeader>
    </Card>
  );
}